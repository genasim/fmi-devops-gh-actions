name: Build and lint Gradle project
on:
  push:
    branches: ["main"]

jobs:
  Editorconfig-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: editorconfig-checker/action-editorconfig-checker@main
      - run: editorconfig-checker

  Markdown-linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Run markdownlint
        run: npx --yes markdownlint-cli@latest "**/*.md" --ignore "node_modules" --ignore "dist"

  Test:
    runs-on: ubuntu-latest
    needs: [Editorconfig-checker, Markdown-linter]
    container: gradle:6-jdk11
    steps:
      - name: Download code
        uses: actions/download-artifact@v4
        with:
          name: code
          path: .
      - name: Test with Gradle
        run: chmod +x ci/unit-test-app.sh && ci/unit-test-app.sh

  Build:
    runs-on: ubuntu-latest
    needs: [Editorconfig-checker, Markdown-linter]
    container: gradle:6-jdk11
    steps:
      - name: Download code
        uses: actions/download-artifact@v4
        with:
          name: code
          path: .
      - name: Build with Gradle
        run: chmod +x ci/build-app.sh && ci/build-app.sh
      - name: Upload Repo
        uses: actions/upload-artifact@v4
        with:
          name: code
          path: .

  # Linter:
  #   runs-on: ubuntu-latest
  #   needs: [Editorconfig-checker, Markdown-linter]

  # Docker-image:
  #   runs-on: ubuntu-latest
  #   needs: [Build,Test]
  #   steps:
  #   - name: Download code
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: code
  #       path: .
  #   - name: build docker
  #     run: chmod +x ci/build-docker.sh && export GIT_COMMIT="GA-$GITHUB_SHA" && ci/build-docker.sh
  #   - name: push docker
  #     run: chmod +x ci/push-docker.sh && export GIT_COMMIT="GA-$GITHUB_SHA" && ci/push-docker.sh
